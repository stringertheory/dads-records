# get all releases from collection that are not a verified pressing

# for each release, use the master release ID to get all other
# releases that share the same master

# check to see if any of the releases are super valuable

from remove_duplicates import get_all_releases

result = get_all_releases()
breakpoint()
